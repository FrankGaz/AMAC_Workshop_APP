import React from "react";

const VehicleExpensesTableItem = ({item}) => {


  
    return (
        <div>

        
      {/* <tr key={item.id}>
        <td>{(item && item.amount) || ''}</td>
        <td>{(item && item.fuel_card_product_kind && item.fuel_card_product_kind.name) || ''}</td>
        <td>{(item && item.input_date) || ''}</td>        
      </tr> */}
      </div>
    );
  };

  export default VehicleExpensesTableItem;